package com.strangerchat.dto;

/**
 * Gender selection for active-user statistics tracking.
 * Only MALE and FEMALE are supported — no additional options.
 */
public enum Gender {
    MALE,
    FEMALE
}
